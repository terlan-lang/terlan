@page {
  title = "Documentation"
  description = "Learn the Terlan language and toolchain from first principles."
  section = "Documentation"
  nav_title = "Docs"
  weight = 10
  kind = "docs"
  layout = "DocsLayout"
}

Start here for the shortest path from a clean machine to a working Terlan
program, then use the language guide as a map of the core syntax and types.

## Guides

- [Getting started](docs/getting-started/) — install the compiler and build a
  first project.
- [Language guide](docs/language/) — explore modules, functions, values, and
  patterns.

This index is part of the generated navigation graph. Adding a child page only
requires `parent` and `weight` metadata in that page's Markdown header.
