/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Dom.CanvasImageData.

import type std.core.{Unit}.

import type std.js.Number.{JsNumber}.

pub opaque type CanvasImageData.

/**
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/CanvasRenderingContext2D/createImageData)
 */
pub (value: CanvasImageData) create_image_data(sw: JsNumber, sh: JsNumber, settings: ImageDataSettings): ImageData ->
    native.

pub (value: CanvasImageData) create_image_data(image_data: ImageData): ImageData ->
    native.

/**
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/CanvasRenderingContext2D/getImageData)
 */
pub (value: CanvasImageData) get_image_data(sx: JsNumber, sy: JsNumber, sw: JsNumber, sh: JsNumber, settings: ImageDataSettings): ImageData ->
    native.

/**
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/CanvasRenderingContext2D/putImageData)
 */
pub (value: CanvasImageData) put_image_data(image_data: ImageData, dx: JsNumber, dy: JsNumber): Unit ->
    native.

pub (value: CanvasImageData) put_image_data(image_data: ImageData, dx: JsNumber, dy: JsNumber, dirty_x: JsNumber, dirty_y: JsNumber, dirty_width: JsNumber, dirty_height: JsNumber): Unit ->
    native.
