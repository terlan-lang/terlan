/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Dom.CanvasDrawImageTest.

import type std.core.{Unit}.

import type std.js.Dom.CanvasDrawImage.{CanvasDrawImage}.

import type std.js.Number.{JsNumber}.

@test
pub generated_binding_surface_exists(): Bool ->
    true.

pub draw_image_typechecks(receiver: CanvasDrawImage, image: CanvasImageSource, dx: JsNumber, dy: JsNumber): Unit ->
    receiver.draw_image(image, dx, dy).

pub draw_image_typechecks(receiver: CanvasDrawImage, image: CanvasImageSource, dx: JsNumber, dy: JsNumber, dw: JsNumber, dh: JsNumber): Unit ->
    receiver.draw_image(image, dx, dy, dw, dh).

pub draw_image_typechecks(receiver: CanvasDrawImage, image: CanvasImageSource, sx: JsNumber, sy: JsNumber, sw: JsNumber, sh: JsNumber, dx: JsNumber, dy: JsNumber, dw: JsNumber, dh: JsNumber): Unit ->
    receiver.draw_image(image, sx, sy, sw, sh, dx, dy, dw, dh).
