/**
 * @generated true
 * @do-not-edit true
 * @generator terlc
 * @generator-version 0.0.8
 * @generator-profile typescript-standard-js-dom
 * @artifact-kind test
 * @input-manifest std/js/manifests/std_js_dom_inputs.json
 * @source-package typescript@5.9.3
 * @source-input std/js/fixtures/lib.es5.d.ts,std/js/fixtures/lib.es2015.collection.d.ts,std/js/fixtures/lib.dom.d.ts
 * @source-interface CanvasDrawImage
 */
module std.js.Dom.CanvasDrawImageTest.

import type std.core.{Unit}.
import type std.js.Dom.CanvasDrawImage.
import type std.js.Number.{JsNumber}.

pub generated_binding_surface_contract(): Bool -> true.

pub draw_image_typechecks(
    receiver: CanvasDrawImage,
    image: CanvasImageSource,
    dx: JsNumber,
    dy: JsNumber,
): Unit ->
    receiver.draw_image(image, dx, dy).

pub draw_image_typechecks(
    receiver: CanvasDrawImage,
    image: CanvasImageSource,
    dx: JsNumber,
    dy: JsNumber,
    dw: JsNumber,
    dh: JsNumber,
): Unit ->
    receiver.draw_image(image, dx, dy, dw, dh).

pub draw_image_typechecks(
    receiver: CanvasDrawImage,
    image: CanvasImageSource,
    sx: JsNumber,
    sy: JsNumber,
    sw: JsNumber,
    sh: JsNumber,
    dx: JsNumber,
    dy: JsNumber,
    dw: JsNumber,
    dh: JsNumber,
): Unit ->
    receiver.draw_image(image, sx, sy, sw, sh, dx, dy, dw, dh).
