/**
 * @generated true
 * @do-not-edit true
 * @generator terlc
 * @generator-version 0.0.7
 * @generator-profile typescript-standard-js-dom
 * @artifact-kind source
 * @input-manifest std/js/manifests/std_js_dom_inputs.json
 * @source-package typescript@5.9.3
 * @source-input std/js/fixtures/lib.es5.d.ts,std/js/fixtures/lib.es2015.collection.d.ts,std/js/fixtures/lib.dom.d.ts
 * @source-interface EffectTiming
 */
module std.js.Dom.EffectTiming.

import type std.js.Number.{JsNumber}.
import type std.js.String.{JsString}.

pub opaque type EffectTiming.

pub (value: EffectTiming) delay(): JsNumber ->
    native.

pub (value: EffectTiming) direction(): PlaybackDirection ->
    native.

pub (value: EffectTiming) duration(): CSSNumericValue | JsNumber | JsString ->
    native.

pub (value: EffectTiming) easing(): JsString ->
    native.

pub (value: EffectTiming) end_delay(): JsNumber ->
    native.

pub (value: EffectTiming) fill(): FillMode ->
    native.

pub (value: EffectTiming) iteration_start(): JsNumber ->
    native.

pub (value: EffectTiming) iterations(): JsNumber ->
    native.

pub (value: EffectTiming) playback_rate(): JsNumber ->
    native.
