/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Dom.VTTRegion.

import type std.js.Number.{JsNumber}.

/**
 * The `VTTRegion` interface of the WebVTT API describes a portion of the video to render a VTTCue onto.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/VTTRegion)
 */
pub opaque type VTTRegion.

pub (value: VTTRegion) id(): std.js.String.JsString ->
    native.

pub (value: VTTRegion) lines(): JsNumber ->
    native.

pub (value: VTTRegion) region_anchor_x(): JsNumber ->
    native.

pub (value: VTTRegion) region_anchor_y(): JsNumber ->
    native.

pub (value: VTTRegion) scroll(): ScrollSetting ->
    native.

pub (value: VTTRegion) viewport_anchor_x(): JsNumber ->
    native.

pub (value: VTTRegion) viewport_anchor_y(): JsNumber ->
    native.

pub (value: VTTRegion) width(): JsNumber ->
    native.
