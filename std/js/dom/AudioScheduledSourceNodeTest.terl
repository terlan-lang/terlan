/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Dom.AudioScheduledSourceNodeTest.

import type std.core.{Bool}.

import type std.core.{Unit}.

import type std.js.Dom.AudioScheduledSourceNode.{AudioScheduledSourceNode}.

import type std.js.Number.{JsNumber}.

import type std.js.String.{JsString}.

@test
pub generated_binding_surface_exists(): Bool ->
    true.

pub start_typechecks(receiver: AudioScheduledSourceNode, when_: JsNumber): Unit ->
    receiver.start(when_).

pub stop_typechecks(receiver: AudioScheduledSourceNode, when_: JsNumber): Unit ->
    receiver.stop(when_).

pub add_event_listener_typechecks(receiver: AudioScheduledSourceNode, type_: JsString, listener: EventListenerOrEventListenerObject, options: AddEventListenerOptions | Bool): Unit ->
    receiver.add_event_listener(type_, listener, options).

pub remove_event_listener_typechecks(receiver: AudioScheduledSourceNode, type_: JsString, listener: EventListenerOrEventListenerObject, options: EventListenerOptions | Bool): Unit ->
    receiver.remove_event_listener(type_, listener, options).
