/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Dom.IDBCursorTest.

import type std.core.{Unit}.

import type std.js.Dom.IDBCursor.{IDBCursor}.

@test
pub generated_binding_surface_exists(): Bool ->
    true.

pub direction_typechecks(receiver: IDBCursor): IDBCursorDirection ->
    receiver.direction().

pub key_typechecks(receiver: IDBCursor): IDBValidKey ->
    receiver.key().

pub primary_key_typechecks(receiver: IDBCursor): IDBValidKey ->
    receiver.primary_key().

pub request_typechecks(receiver: IDBCursor): IDBRequest ->
    receiver.request().

pub source_typechecks(receiver: IDBCursor): IDBIndex | IDBObjectStore ->
    receiver.source().

pub advance_typechecks(receiver: IDBCursor, count: std.js.Number.JsNumber): Unit ->
    receiver.advance(count).

pub continue_typechecks(receiver: IDBCursor, key: IDBValidKey): Unit ->
    receiver.continue(key).

pub continue_primary_key_typechecks(receiver: IDBCursor, key: IDBValidKey, primary_key: IDBValidKey): Unit ->
    receiver.continue_primary_key(key, primary_key).
