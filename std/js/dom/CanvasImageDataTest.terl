/**
 * @generated true
 * @do-not-edit true
 * @generator terlc
 * @generator-version 0.0.8
 * @generator-profile typescript-standard-js-dom
 * @artifact-kind test
 * @input-manifest std/js/manifests/std_js_dom_inputs.json
 * @source-package typescript@5.9.3
 * @source-input std/js/fixtures/lib.es5.d.ts,std/js/fixtures/lib.es2015.collection.d.ts,std/js/fixtures/lib.dom.d.ts
 * @source-interface CanvasImageData
 */
module std.js.Dom.CanvasImageDataTest.

import type std.core.{Unit}.
import type std.js.Dom.CanvasImageData.
import type std.js.Number.{JsNumber}.

pub generated_binding_surface_contract(): Bool -> true.

pub create_image_data_typechecks(
    receiver: CanvasImageData,
    sw: JsNumber,
    sh: JsNumber,
    settings: ImageDataSettings,
): ImageData ->
    receiver.create_image_data(sw, sh, settings).

pub create_image_data_typechecks(receiver: CanvasImageData, image_data: ImageData): ImageData ->
    receiver.create_image_data(image_data).

pub get_image_data_typechecks(
    receiver: CanvasImageData,
    sx: JsNumber,
    sy: JsNumber,
    sw: JsNumber,
    sh: JsNumber,
    settings: ImageDataSettings,
): ImageData ->
    receiver.get_image_data(sx, sy, sw, sh, settings).

pub put_image_data_typechecks(
    receiver: CanvasImageData,
    image_data: ImageData,
    dx: JsNumber,
    dy: JsNumber,
): Unit ->
    receiver.put_image_data(image_data, dx, dy).

pub put_image_data_typechecks(
    receiver: CanvasImageData,
    image_data: ImageData,
    dx: JsNumber,
    dy: JsNumber,
    dirty_x: JsNumber,
    dirty_y: JsNumber,
    dirty_width: JsNumber,
    dirty_height: JsNumber,
): Unit ->
    receiver.put_image_data(image_data, dx, dy, dirty_x, dirty_y, dirty_width, dirty_height).
