/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Dom.CanvasImageDataTest.

import type std.core.{Unit}.

import type std.js.Dom.CanvasImageData.{CanvasImageData}.

import type std.js.Number.{JsNumber}.

@test
pub generated_binding_surface_exists(): Bool ->
    true.

pub create_image_data_typechecks(receiver: CanvasImageData, sw: JsNumber, sh: JsNumber, settings: ImageDataSettings): ImageData ->
    receiver.create_image_data(sw, sh, settings).

pub create_image_data_typechecks(receiver: CanvasImageData, image_data: ImageData): ImageData ->
    receiver.create_image_data(image_data).

pub get_image_data_typechecks(receiver: CanvasImageData, sx: JsNumber, sy: JsNumber, sw: JsNumber, sh: JsNumber, settings: ImageDataSettings): ImageData ->
    receiver.get_image_data(sx, sy, sw, sh, settings).

pub put_image_data_typechecks(receiver: CanvasImageData, image_data: ImageData, dx: JsNumber, dy: JsNumber): Unit ->
    receiver.put_image_data(image_data, dx, dy).

pub put_image_data_typechecks(receiver: CanvasImageData, image_data: ImageData, dx: JsNumber, dy: JsNumber, dirty_x: JsNumber, dirty_y: JsNumber, dirty_width: JsNumber, dirty_height: JsNumber): Unit ->
    receiver.put_image_data(image_data, dx, dy, dirty_x, dirty_y, dirty_width, dirty_height).
