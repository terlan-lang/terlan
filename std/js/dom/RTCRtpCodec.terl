/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Dom.RTCRtpCodec.

import type std.js.Number.{JsNumber}.

import type std.js.String.{JsString}.

pub opaque type RTCRtpCodec.

pub (value: RTCRtpCodec) channels(): JsNumber ->
    native.

pub (value: RTCRtpCodec) clock_rate(): JsNumber ->
    native.

pub (value: RTCRtpCodec) mime_type(): JsString ->
    native.

pub (value: RTCRtpCodec) sdp_fmtp_line(): JsString ->
    native.
