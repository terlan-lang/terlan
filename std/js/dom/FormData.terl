/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Dom.FormData.

import type std.core.{Unit}.

import type std.js.String.{JsString}.

/**
 * The **`FormData`** interface provides a way to construct a set of key/value pairs representing form fields and their values, which can be sent using the Window/fetch, XMLHttpRequest.send() or navigator.sendBeacon() methods.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/FormData)
 */
pub opaque type FormData.

/**
 * The **`append()`** method of the FormData interface appends a new value onto an existing key inside a `FormData` object, or adds the key if it does not already exist.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/FormData/append)
 */
pub (value: FormData) append(name: JsString, value: Blob | JsString): Unit ->
    native.

pub (value: FormData) append(name: JsString, value: JsString): Unit ->
    native.

pub (value: FormData) append(name: JsString, blob_value: Blob, filename: JsString): Unit ->
    native.

/**
 * The **`delete()`** method of the FormData interface deletes a key and its value(s) from a `FormData` object.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/FormData/delete)
 */
pub (value: FormData) delete(name: JsString): Unit ->
    native.

/**
 * The **`get()`** method of the FormData interface returns the first value associated with a given key from within a `FormData` object.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/FormData/get)
 */
pub (value: FormData) get(name: JsString): Option[FormDataEntryValue] ->
    native.

/**
 * The **`getAll()`** method of the FormData interface returns all the values associated with a given key from within a `FormData` object.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/FormData/getAll)
 */
pub (value: FormData) get_all(name: JsString): std.js.Array[FormDataEntryValue] ->
    native.

/**
 * The **`has()`** method of the FormData interface returns whether a `FormData` object contains a certain key.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/FormData/has)
 */
pub (value: FormData) has(name: JsString): std.core.Bool ->
    native.

/**
 * The **`set()`** method of the FormData interface sets a new value for an existing key inside a `FormData` object, or adds the key/value if it does not already exist.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/FormData/set)
 */
pub (value: FormData) set(name: JsString, value: Blob | JsString): Unit ->
    native.

pub (value: FormData) set(name: JsString, value: JsString): Unit ->
    native.

pub (value: FormData) set(name: JsString, blob_value: Blob, filename: JsString): Unit ->
    native.
