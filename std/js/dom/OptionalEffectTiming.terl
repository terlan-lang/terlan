/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Dom.OptionalEffectTiming.

import type std.js.Number.{JsNumber}.

import type std.js.String.{JsString}.

pub opaque type OptionalEffectTiming.

pub (value: OptionalEffectTiming) delay(): JsNumber ->
    native.

pub (value: OptionalEffectTiming) direction(): PlaybackDirection ->
    native.

pub (value: OptionalEffectTiming) duration(): JsNumber | JsString ->
    native.

pub (value: OptionalEffectTiming) easing(): JsString ->
    native.

pub (value: OptionalEffectTiming) end_delay(): JsNumber ->
    native.

pub (value: OptionalEffectTiming) fill(): FillMode ->
    native.

pub (value: OptionalEffectTiming) iteration_start(): JsNumber ->
    native.

pub (value: OptionalEffectTiming) iterations(): JsNumber ->
    native.

pub (value: OptionalEffectTiming) playback_rate(): JsNumber ->
    native.
