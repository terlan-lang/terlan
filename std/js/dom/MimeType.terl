/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Dom.MimeType.

import type std.js.String.{JsString}.

/**
 * The **`MimeType`** interface provides contains information about a MIME type associated with a particular plugin.
 * @deprecated
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/MimeType)
 */
pub opaque type MimeType.

/**
 * Returns the MIME type's description.
 * @deprecated
 */
pub (value: MimeType) description(): JsString ->
    native.

/**
 * Returns the Plugin object that implements this MIME type.
 * @deprecated
 */
pub (value: MimeType) enabled_plugin(): Plugin ->
    native.

/**
 * Returns the MIME type's typical file extensions, in a comma-separated list.
 * @deprecated
 */
pub (value: MimeType) suffixes(): JsString ->
    native.

/**
 * Returns the MIME type.
 * @deprecated
 */
pub (value: MimeType) type_(): JsString ->
    native.
