/**
 * @generated true
 * @do-not-edit true
 * @generator terlc
 * @generator-version 0.0.7
 * @generator-profile typescript-standard-js-dom
 * @artifact-kind source
 * @input-manifest std/js/manifests/std_js_dom_inputs.json
 * @source-package typescript@5.9.3
 * @source-input std/js/fixtures/lib.es5.d.ts,std/js/fixtures/lib.es2015.collection.d.ts,std/js/fixtures/lib.dom.d.ts
 * @source-interface SubtleCrypto
 */
module std.js.Dom.SubtleCrypto.

import type std.core.{Bool}.
import type std.js.{Array, Promise}.

/**
 * The **`SubtleCrypto`** interface of the Web Crypto API provides a number of low-level cryptographic functions.
 * Available only in secure contexts.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/SubtleCrypto)
 */
pub opaque type SubtleCrypto.

/**
 * The **`decrypt()`** method of the SubtleCrypto interface decrypts some encrypted data.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/SubtleCrypto/decrypt)
 */
pub (value: SubtleCrypto) decrypt(
    algorithm: AesCbcParams | AesCtrParams | AesGcmParams | AlgorithmIdentifier | RsaOaepParams,
    key: CryptoKey,
    data: BufferSource
): Promise[ArrayBuffer] ->
    native.

/**
 * The **`deriveBits()`** method of the key.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/SubtleCrypto/deriveBits)
 */
pub (value: SubtleCrypto) derive_bits(
    algorithm: AlgorithmIdentifier | EcdhKeyDeriveParams | HkdfParams | Pbkdf2Params,
    base_key: CryptoKey,
    length: Option[std.js.Number.JsNumber]
): Promise[ArrayBuffer] ->
    native.

/**
 * The **`deriveKey()`** method of the SubtleCrypto interface can be used to derive a secret key from a master key.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/SubtleCrypto/deriveKey)
 */
pub (value: SubtleCrypto) derive_key(
    algorithm: AlgorithmIdentifier | EcdhKeyDeriveParams | HkdfParams | Pbkdf2Params,
    base_key: CryptoKey,
    derived_key_type: AesDerivedKeyParams | AlgorithmIdentifier | HkdfParams | HmacImportParams | Pbkdf2Params,
    extractable: Bool,
    key_usages: Array[KeyUsage]
): Promise[CryptoKey] ->
    native.

/**
 * The **`digest()`** method of the SubtleCrypto interface generates a _digest_ of the given data, using the specified hash function.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/SubtleCrypto/digest)
 */
pub (value: SubtleCrypto) digest(algorithm: AlgorithmIdentifier, data: BufferSource): Promise[ArrayBuffer] ->
    native.

/**
 * The **`encrypt()`** method of the SubtleCrypto interface encrypts data.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/SubtleCrypto/encrypt)
 */
pub (value: SubtleCrypto) encrypt(
    algorithm: AesCbcParams | AesCtrParams | AesGcmParams | AlgorithmIdentifier | RsaOaepParams,
    key: CryptoKey,
    data: BufferSource
): Promise[ArrayBuffer] ->
    native.

pub (value: SubtleCrypto) export_key(format: KeyFormat, key: CryptoKey): Promise[ArrayBuffer | JsonWebKey] ->
    native.

pub (value: SubtleCrypto) generate_key(
    algorithm: EcKeyGenParams | RsaHashedKeyGenParams,
    extractable: Bool,
    key_usages: Array[KeyUsage]
): Promise[CryptoKeyPair] ->
    native.

pub (value: SubtleCrypto) generate_key(
    algorithm: AesKeyGenParams | HmacKeyGenParams | Pbkdf2Params,
    extractable: Bool,
    key_usages: Array[KeyUsage]
): Promise[CryptoKey] ->
    native.

pub (value: SubtleCrypto) generate_key(
    algorithm: AlgorithmIdentifier,
    extractable: Bool,
    key_usages: Array[KeyUsage]
): Promise[CryptoKey | CryptoKeyPair] ->
    native.

/**
 * The **`sign()`** method of the SubtleCrypto interface generates a digital signature.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/SubtleCrypto/sign)
 */
pub (value: SubtleCrypto) sign(
    algorithm: AlgorithmIdentifier | EcdsaParams | RsaPssParams,
    key: CryptoKey,
    data: BufferSource
): Promise[ArrayBuffer] ->
    native.

/**
 * The **`unwrapKey()`** method of the SubtleCrypto interface 'unwraps' a key.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/SubtleCrypto/unwrapKey)
 */
pub (value: SubtleCrypto) unwrap_key(
    format: KeyFormat,
    wrapped_key: BufferSource,
    unwrapping_key: CryptoKey,
    unwrap_algorithm: AesCbcParams | AesCtrParams | AesGcmParams | AlgorithmIdentifier | RsaOaepParams,
    unwrapped_key_algorithm: AesKeyAlgorithm | AlgorithmIdentifier | EcKeyImportParams | HmacImportParams | RsaHashedImportParams,
    extractable: Bool,
    key_usages: Array[KeyUsage]
): Promise[CryptoKey] ->
    native.

/**
 * The **`verify()`** method of the SubtleCrypto interface verifies a digital signature.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/SubtleCrypto/verify)
 */
pub (value: SubtleCrypto) verify(
    algorithm: AlgorithmIdentifier | EcdsaParams | RsaPssParams,
    key: CryptoKey,
    signature: BufferSource,
    data: BufferSource
): Promise[Bool] ->
    native.

/**
 * The **`wrapKey()`** method of the SubtleCrypto interface 'wraps' a key.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/SubtleCrypto/wrapKey)
 */
pub (value: SubtleCrypto) wrap_key(
    format: KeyFormat,
    key: CryptoKey,
    wrapping_key: CryptoKey,
    wrap_algorithm: AesCbcParams | AesCtrParams | AesGcmParams | AlgorithmIdentifier | RsaOaepParams
): Promise[ArrayBuffer] ->
    native.
