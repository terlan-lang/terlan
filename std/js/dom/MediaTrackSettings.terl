/**
 * @generated true
 * @do-not-edit true
 * @generator terlc
 * @generator-version 0.0.8
 * @generator-profile typescript-standard-js-dom
 * @artifact-kind source
 * @input-manifest std/js/manifests/std_js_dom_inputs.json
 * @source-package typescript@5.9.3
 * @source-input std/js/fixtures/lib.es5.d.ts,std/js/fixtures/lib.es2015.collection.d.ts,std/js/fixtures/lib.dom.d.ts
 * @source-interface MediaTrackSettings
 */
module std.js.Dom.MediaTrackSettings.

import type std.core.{Bool}.
import type std.js.Number.{JsNumber}.
import type std.js.String.{JsString}.

pub opaque type MediaTrackSettings.

pub (value: MediaTrackSettings) aspect_ratio(): JsNumber ->
    native.

pub (value: MediaTrackSettings) auto_gain_control(): Bool ->
    native.

pub (value: MediaTrackSettings) background_blur(): Bool ->
    native.

pub (value: MediaTrackSettings) channel_count(): JsNumber ->
    native.

pub (value: MediaTrackSettings) device_id(): JsString ->
    native.

pub (value: MediaTrackSettings) display_surface(): JsString ->
    native.

pub (value: MediaTrackSettings) echo_cancellation(): Bool ->
    native.

pub (value: MediaTrackSettings) facing_mode(): JsString ->
    native.

pub (value: MediaTrackSettings) frame_rate(): JsNumber ->
    native.

pub (value: MediaTrackSettings) group_id(): JsString ->
    native.

pub (value: MediaTrackSettings) height(): JsNumber ->
    native.

pub (value: MediaTrackSettings) noise_suppression(): Bool ->
    native.

pub (value: MediaTrackSettings) sample_rate(): JsNumber ->
    native.

pub (value: MediaTrackSettings) sample_size(): JsNumber ->
    native.

pub (value: MediaTrackSettings) torch(): Bool ->
    native.

pub (value: MediaTrackSettings) white_balance_mode(): JsString ->
    native.

pub (value: MediaTrackSettings) width(): JsNumber ->
    native.

pub (value: MediaTrackSettings) zoom(): JsNumber ->
    native.
