/**
 * @generated true
 * @do-not-edit true
 * @generator terlc
 * @generator-version 0.0.7
 * @generator-profile typescript-standard-js-dom
 * @artifact-kind interface
 * @input-manifest std/js/manifests/std_js_dom_inputs.json
 * @source-package typescript@5.9.3
 * @source-input std/js/fixtures/lib.es5.d.ts,std/js/fixtures/lib.es2015.collection.d.ts,std/js/fixtures/lib.dom.d.ts
 * @source-interface EventTarget
 */
module std.js.Dom.EventTarget.

import type std.core.{Bool, Unit}.
import type std.js.String.{JsString}.

/**
 * The **`EventTarget`** interface is implemented by objects that can receive events and may have listeners for them.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventTarget)
 */
pub opaque type EventTarget.

/**
 * The **`addEventListener()`** method of the EventTarget interface sets up a function that will be called whenever the specified event is delivered to the target.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventTarget/addEventListener)
 */
pub (value: EventTarget) add_event_listener(
    type_: JsString,
    callback: Option[EventListenerOrEventListenerObject],
    options: AddEventListenerOptions | Bool,
): Unit.

/**
 * The **`dispatchEvent()`** method of the EventTarget sends an Event to the object, (synchronously) invoking the affected event listeners in the appropriate order.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventTarget/dispatchEvent)
 */
pub (value: EventTarget) dispatch_event(event: Event): Bool.

/**
 * The **`removeEventListener()`** method of the EventTarget interface removes an event listener previously registered with EventTarget.addEventListener() from the target.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventTarget/removeEventListener)
 */
pub (value: EventTarget) remove_event_listener(
    type_: JsString,
    callback: Option[EventListenerOrEventListenerObject],
    options: EventListenerOptions | Bool,
): Unit.
