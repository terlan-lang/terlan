/**
 * @generated true
 * @do-not-edit true
 * @generator terlc
 * @generator-version 0.0.8
 * @generator-profile typescript-standard-js-dom
 * @artifact-kind source
 * @input-manifest std/js/manifests/std_js_dom_inputs.json
 * @source-package typescript@5.9.3
 * @source-input std/js/fixtures/lib.es5.d.ts,std/js/fixtures/lib.es2015.collection.d.ts,std/js/fixtures/lib.dom.d.ts
 * @source-interface Uint8ArrayConstructor
 */
module std.js.Uint8ArrayConstructor.

import type std.js.Number.{JsNumber}.

pub opaque type Uint8ArrayConstructor.

pub (value: Uint8ArrayConstructor) prototype(): Uint8Array[ArrayBufferLike] ->
    native.

/**
 * The size in bytes of each element in the array.
 */
pub (value: Uint8ArrayConstructor) bytes_per_element(): JsNumber ->
    native.

/**
 * Returns a new array from a set of elements.
 * @param items A set of elements to include in the new array object.
 */
pub (value: Uint8ArrayConstructor) of(): Uint8Array[ArrayBuffer] ->
    native.

/**
 * Creates an array from an array-like or iterable object.
 * @param arrayLike An array-like object to convert to an array.
 */
pub (value: Uint8ArrayConstructor) from_(array_like: ArrayLike[JsNumber]): Uint8Array[ArrayBuffer] ->
    native.
