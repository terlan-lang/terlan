/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Int16ArrayConstructor.

import type std.js.Number.{JsNumber}.

pub opaque type Int16ArrayConstructor.

pub (value: Int16ArrayConstructor) prototype(): Int16Array[ArrayBufferLike] ->
    native.

/**
 * The size in bytes of each element in the array.
 */
pub (value: Int16ArrayConstructor) bytes_per_element(): JsNumber ->
    native.

/**
 * Returns a new array from a set of elements.
 * @param items A set of elements to include in the new array object.
 */
pub (value: Int16ArrayConstructor) of(): Int16Array[ArrayBuffer] ->
    native.

/**
 * Creates an array from an array-like or iterable object.
 * @param arrayLike An array-like object to convert to an array.
 */
pub (value: Int16ArrayConstructor) from_(array_like: ArrayLike[JsNumber]): Int16Array[ArrayBuffer] ->
    native.
