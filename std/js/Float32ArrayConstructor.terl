/**
 * @generated true
 * @do-not-edit true
 */
module std.js.Float32ArrayConstructor.

import type std.js.Number.{JsNumber}.

pub opaque type Float32ArrayConstructor.

pub (value: Float32ArrayConstructor) prototype(): Float32Array[ArrayBufferLike] ->
    native.

/**
 * The size in bytes of each element in the array.
 */
pub (value: Float32ArrayConstructor) bytes_per_element(): JsNumber ->
    native.

/**
 * Returns a new array from a set of elements.
 * @param items A set of elements to include in the new array object.
 */
pub (value: Float32ArrayConstructor) of(): Float32Array[ArrayBuffer] ->
    native.

/**
 * Creates an array from an array-like or iterable object.
 * @param arrayLike An array-like object to convert to an array.
 */
pub (value: Float32ArrayConstructor) from_(array_like: ArrayLike[JsNumber]): Float32Array[ArrayBuffer] ->
    native.
