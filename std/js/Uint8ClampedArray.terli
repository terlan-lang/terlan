/**
 * @generated true
 * @do-not-edit true
 * @generator terlc
 * @generator-version 0.0.7
 * @generator-profile typescript-standard-js-dom
 * @artifact-kind interface
 * @input-manifest std/js/manifests/std_js_dom_inputs.json
 * @source-package typescript@5.9.3
 * @source-input std/js/fixtures/lib.es5.d.ts,std/js/fixtures/lib.es2015.collection.d.ts,std/js/fixtures/lib.dom.d.ts
 * @source-interface Uint8ClampedArray
 */
module std.js.Uint8ClampedArray.

import type std.js.Number.{JsNumber}.
import type std.js.String.{JsString}.

/**
 * A typed array of 8-bit unsigned integer (clamped) values. The contents are initialized to 0.
 * If the requested number of bytes could not be allocated an exception is raised.
 */
pub opaque type Uint8ClampedArray[TArrayBuffer].

/**
 * The size in bytes of each element in the array.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) bytes_per_element(): JsNumber.

/**
 * The ArrayBuffer instance referenced by the array.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) buffer(): TArrayBuffer.

/**
 * The length in bytes of the array.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) byte_length(): JsNumber.

/**
 * The offset in bytes of the array.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) byte_offset(): JsNumber.

/**
 * Returns the index of the first occurrence of a value in an array.
 * @param searchElement The value to locate in the array.
 * @param fromIndex The array index at which to begin the search. If fromIndex is omitted, the
 * search starts at index 0.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) index_of(search_element: JsNumber, from_index: JsNumber): JsNumber.

/**
 * Adds all the elements of an array separated by the specified separator string.
 * @param separator A string used to separate one element of an array from the next in the
 * resulting String. If omitted, the array elements are separated with a comma.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) join(separator: JsString): JsString.

/**
 * Returns the index of the last occurrence of a value in an array.
 * @param searchElement The value to locate in the array.
 * @param fromIndex The array index at which to begin the search. If fromIndex is omitted, the
 * search starts at index 0.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) last_index_of(search_element: JsNumber, from_index: JsNumber): JsNumber.

/**
 * The length of the array.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) length(): JsNumber.

/**
 * Sets a value or an array of values.
 * @param array A typed or untyped array of values to set.
 * @param offset The index in the current array at which the values are to be written.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) set(array: ArrayLike[JsNumber], offset: JsNumber): std.core.Unit.

/**
 * Returns a section of an array.
 * @param start The beginning of the specified portion of the array.
 * @param end The end of the specified portion of the array. This is exclusive of the element at the index 'end'.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) slice(start: JsNumber, end: JsNumber): Uint8ClampedArray[ArrayBuffer].

/**
 * Gets a new Uint8ClampedArray view of the ArrayBuffer store for this array, referencing the elements
 * at begin, inclusive, up to end, exclusive.
 * @param begin The index of the beginning of the array.
 * @param end The index of the end of the array.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) subarray(begin: JsNumber, end: JsNumber): Uint8ClampedArray[TArrayBuffer].

/**
 * Converts a number to a string by using the current locale.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) to_locale_string(): JsString.

/**
 * Returns a string representation of an array.
 */
pub (value: Uint8ClampedArray[TArrayBuffer]) to_string(): JsString.
