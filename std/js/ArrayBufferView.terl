/**
 * @generated true
 * @do-not-edit true
 */
module std.js.ArrayBufferView.

import type std.js.Number.{JsNumber}.

pub opaque type ArrayBufferView[TArrayBuffer].

/**
 * The ArrayBuffer instance referenced by the array.
 */
pub (value: ArrayBufferView[TArrayBuffer]) buffer(): TArrayBuffer ->
    native.

/**
 * The length in bytes of the array.
 */
pub (value: ArrayBufferView[TArrayBuffer]) byte_length(): JsNumber ->
    native.

/**
 * The offset in bytes of the array.
 */
pub (value: ArrayBufferView[TArrayBuffer]) byte_offset(): JsNumber ->
    native.
