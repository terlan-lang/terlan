[package]
name = "terlan_wasm"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
wasm-encoder = "0.252.0"
wasmparser = "0.252.0"

[dev-dependencies]
wat = "1.252.0"
