name: Compiler CI

on:
  pull_request:
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "crates/**"
      - "std/**"
      - "tests/**"
      - "docs/grammar/**"
      - "tools/**"
      - "scripts/**"
      - "install.sh"
      - "Makefile"
      - ".github/workflows/ci.yml"
      - ".github/workflows/release.yml"
  push:
    branches:
      - main
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "crates/**"
      - "std/**"
      - "tests/**"
      - "docs/grammar/**"
      - "tools/**"
      - "scripts/**"
      - "install.sh"
      - "Makefile"
      - ".github/workflows/ci.yml"
      - ".github/workflows/release.yml"

permissions:
  contents: read

jobs:
  check:
    name: Compiler check and test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        run: rustup toolchain install stable --profile minimal

      - name: Install Erlang/OTP
        uses: erlef/setup-beam@v1
        with:
          otp-version: "29"

      - name: Check
        run: make check

      - name: Test
        run: make test

      - name: JS target gate
        run: make release-0-0-4-preflight
