module html_interpolation.

pub type User =
    {user, Int, Text}.

pub title(User: User): Text ->
    case User {
        {user, _, Name} -> Name
    }.

pub view(User: User): Html[none] ->
    html {
        <h1>{title(User)}</h1>
    }.
