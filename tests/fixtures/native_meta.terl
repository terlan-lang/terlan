module native_meta.

target erlang with safe_native.

pub length[T](A: Array[T]): Int ->
    0.

native core module NativeArray {
    #[native(normal)]
    length[T](A: Array[T]): Int.

    #[native(dirty_cpu)]
    push[T](A: Array[T], Value: T): Array[T].
}
