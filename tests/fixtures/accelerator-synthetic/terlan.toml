[package]
name = "terlan-synthetic-vector"
version = "0.0.9"
namespace = "synthetic_vector"

[accelerator]
schema = 1
descriptor = "accelerator.toml"

[build]
source_roots = ["src"]
artifact = "library"
