module std.core.Secret.SecretRejectsShowTest.

import std.core.Secret.{Secret, new}.
import std.core.String.{Show}.

render[T](value: T)[Show[T]]: String ->
    Show.to_string(value).

@test
pub secret_rejects_show(): Bool ->
    render(new("private")) == "private".
