module std.core.Secret.SecretRejectsEqualTest.

import std.core.Equal.{Equal}.
import std.core.Secret.{Secret, new}.

equal[T](left: T, right: T)[Equal[T]]: Bool ->
    Equal.equal(left, right).

@test
pub secret_rejects_equal(): Bool ->
    let secret = new("private");
    equal(secret, secret).
